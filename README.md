# eCommerce CDP app full stack

## Installation and startup

1. Run `npm i` in the following folders:

- root
- `backend`
- `frontend`

2. In the root folder, create a `.env` file and add the required environment variables.

3. To run both the backend and the frontend: `npm start`
